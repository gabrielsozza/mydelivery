-- ============================================================
-- BASELINE — V1
-- ============================================================
-- Este arquivo existe apenas para marcar o ponto de partida do
-- Flyway. O schema atual em produção foi criado pelo Hibernate
-- (ddl-auto=update) antes da adoção do Flyway.
--
-- A partir daqui, TODA mudança de schema deve vir como nova
-- migration V2__, V3__, ... ordenada por número crescente. Não
-- editar este arquivo retroativamente.
-- ============================================================

SELECT 1;
